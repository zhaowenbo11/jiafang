import type {
  PreviewPromptTemplate,
  PromptTemplateKey,
} from "./types";

export const previewPromptTemplates: Record<
  PromptTemplateKey,
  PreviewPromptTemplate
> = {
  wedding_hero: {
    key: "wedding_hero",
    businessId: "wedding",
    templateType: "hero",
    title: "婚庆主模板 AI 成图提示词",
    summary:
      "用于婚庆主模板，将真实布料应用到整套婚床床品，强调整套统一替换、真实花型分布和门店成交展示感。",
    systemPrompt:
      "你是婚庆家纺门店成品图生成助手。请基于固定婚床模板图和真实布料参考图，生成可用于客户选款确认的高真实度婚庆床品效果图。",
    userPrompt: `
业务类型：婚庆家纺
模板名称：{{templateName}}
场景描述：{{sceneDescription}}

布料信息：
- 布料名称：{{fabricName}}
- 布料分类：{{fabricCategory}}
- 颜色：{{colorName}}
- 材质：{{material}}
- 工艺：{{craft}}

生成目标：
请把参考布料完整应用到婚庆床品中，输出一张真实、统一、适合门店成交展示的婚床成品图。

严格要求：
1. 必须严格保持模板图中的房间结构、床体结构、拍摄角度、构图、床头、床尾、床边柜、背景和灯光关系不变。
2. 必须整套统一替换床品，不允许只替换局部。需要同时替换：
   - 床单
   - 被套正面
   - 左枕套
   - 右枕套
3. 整套床品必须使用同一款布料，颜色、花型、纹理、光泽和面料气质要与参考图保持一致。
4. 花型分布必须像真实家纺成品，不要把图案随意放大、缩小、打散或重新设计。要符合真实床品裁片逻辑和铺床效果。
5. 成品质感要偏高端婚庆门店实拍，既有婚庆氛围，又要克制、耐看、适合成交，不要廉价感，不要海报感。
6. 请保留真实布料褶皱、垂坠、包边、缝线、反光和层次感，让床面看起来像真实做出来并铺好的婚庆成品。
7. 枕套、被面、床单之间要有统一感，不能出现“同款但不像一整套”的割裂效果。
`.trim(),
    negativePrompt:
      "不要只生成床尾一条布，不要床旗，不要床尾巾，不要只改一个枕头，不要新增不相关图案，不要改变房间结构，不要改变床体，不要改变背景，不要改变镜头角度，不要出现构图漂移，不要花型失真，不要图案漂浮，不要过度磨皮，不要明显AI感，不要过度艺术化，不要廉价电商风。",
  },
  custom_hero: {
    key: "custom_hero",
    businessId: "custom",
    templateType: "hero",
    title: "高端定制主模板 AI 成图提示词",
    summary:
      "用于主卧主模板，将真实布料图完整铺到整套床品，强调材质还原和高级成品氛围。",
    systemPrompt:
      "你是高端家纺成品图生成助手。请基于固定主卧模板和真实布料参考图，生成可用于销售沟通的真实床品效果图。",
    userPrompt: `
业务类型：高端手工定制
模板名称：{{templateName}}
场景描述：{{sceneDescription}}

布料信息：
- 布料名称：{{fabricName}}
- 布料分类：{{fabricCategory}}
- 颜色：{{colorName}}
- 材质：{{material}}
- 工艺：{{craft}}

生成要求：
1. 以模板图作为唯一房间与床型模板，严格保持房间结构、床体结构、拍摄角度、构图、床头、床尾、床边柜和背景不变。
2. 将所给布料完整应用到整套床品中，必须统一替换床单、被套正面、左枕套、右枕套。
3. 高度还原布料颜色、花型、纹理、织感、光泽和材质细节，不要生成与参考图无关的新图案。
4. 花型分布、褶皱、缝边、垂坠和反光要符合真实高端家纺成品效果，强调真实感而不是海报感。
5. 输出应像门店样板间实拍，既能体现材质高级感，也能支持客户确认是否继续下单。
`.trim(),
    negativePrompt:
      "不要局部替换，不要床尾只加一条布，不要床旗，不要抱枕主导画面，不要只改部分区域，不要改变空间结构，不要改变床体比例，不要改变镜头角度，不要花型失真，不要过度磨皮，不要明显AI渲染感。",
  },
};
